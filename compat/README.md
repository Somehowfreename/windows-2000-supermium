# Native Windows 2000 bridges

This directory contains only the native bridge sources and DEF files used by the final Candidate 19 release payload. Experimental aliases, probe binaries, rejected candidates and compiled artifacts are deliberately excluded.

The expected output names and hashes are documented in `docs/BUILDING.md`. The bridge DLLs are copied beside Supermium; they do not replace or modify files in the Windows directory.
