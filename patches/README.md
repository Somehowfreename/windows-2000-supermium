# Version-locked production patches

These scripts are the exact production chain for Supermium `144.0.7559.256 R5`. Each script verifies the expected input before writing. The candidate numbers preserve the debugging provenance and execution order; they do not imply that rejected candidates are included.

Candidate 15 added no patch. Candidate 16 was diagnostic-only and rejected. Candidate 20 was a later experiment and is not part of the finalized payload. Their files are intentionally absent.

See `docs/BUILDING.md` for order, expected inputs and final hashes. Never bypass a mismatch on another upstream version.
